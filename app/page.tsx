import VinylHome from "@/components/home/VinylHome";

/**
 * Ruta `/` — única home del sitio: siempre la landing con intro de vinilo (`VinylHome`).
 * Implementación: `@/components/home/VinylHome`.
 */
export default function HomePage() {
  return <VinylHome />;
}
